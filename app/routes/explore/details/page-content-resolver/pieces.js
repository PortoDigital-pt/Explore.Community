import { EventContactDetails } from '../events/page';
import { RoutesDetails } from '../routes/detail';

export const PAGE_CONTENT_PIECES_TYPE_MAP = {
  events: { details: EventContactDetails },
  routes: { details: RoutesDetails }
};
